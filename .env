# Environment variables for Vite (do NOT commit secrets here)
# Update VITE_API_BASE to point to your backend API (example below)
VITE_API_BASE=http://localhost:4000
VITE_API_TIMEOUT_MS=10000
# Set to true if you want to use remote API endpoints
VITE_USE_REMOTE_API=true
